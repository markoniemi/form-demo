Tasks
1. Create RequirementsSpecification.md
    Create requirements specification document for an application for creating and filling forms.
2. Create UseCases.md
   Create use cases document for the form creation and filling application.
3. Create TechnicalSpecification.md
    Create technical specification document for the form creation and filling application. Create a multimodule architecture with frontend, backend. Use RESTful APIs for communication between frontend and backend. Use OAuth 2.0 for authentication and authorization. Use react for frontend, spring boot for backend, and a database for data storage. Package frontend as a jar file using frontend-maven-plugin and use it in the backend module. Create oauth module for handling authentication and authorization during testing. Package oauth module as docker container using jib and use it in the backend module using testcontainers library. Frontend uses fetch, react, vite, bootstrap, typescript. Backend uses spring data jpa, spring security, lombok. Use h2 database for testing. Add LocalDevelopmentApplication for local development with in memory oauth server and h2 database. Create maven multi module project structure.
4. Create ProjectPlan.md
    Create project plan document for the form creation and filling application. Implement application in sprints.
5. Create CodingStandards.md
    Create coding standards document for the form creation and filling application.
6. Implement application
   Implement application based on the created documents in phase-wise manner.